message(STATUS "")

message(STATUS "BENCH_FFT ${PROJECT_VERSION}")

string(TOUPPER "${CMAKE_BUILD_TYPE}" BENCH_FFT_BUILD_TYPE)
set(BENCH_FFT_OPTIONS "CMAKE_INSTALL_PREFIX;BUILD_SHARED_LIBS;CMAKE_BUILD_TYPE;CMAKE_CXX_FLAGS_${BENCH_FFT_BUILD_TYPE};CMAKE_CXX_FLAGS")

if (ENABLE_CUFFT)
    list(APPEND BENCH_FFT_OPTIONS "CUDA_NVCC_FLAGS")
    list(APPEND BENCH_FFT_OPTIONS "CUDA_TOOLKIT_ROOT_DIR")
endif()

foreach(_opt ROCFFT CUFFT FFTW)
    list(APPEND OPTIONS "ENABLE_${_opt}")
endforeach()

foreach(_opt ${OPTIONS})
    message(STATUS " -D ${_opt}=${${_opt}}")
endforeach()
unset(_opt)

message(STATUS "")
